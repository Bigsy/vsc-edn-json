{
  "name": "edn-json-converter",
  "displayName": "EDN-JSON Converter",
  "description": "Convert between EDN and JSON formats with visual highlighting",
  "version": "0.0.1",
  "publisher": "YOUR-PUBLISHER-NAME",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other",
    "Formatters"
  ],
  "keywords": [
    "edn",
    "json",
    "convert",
    "formatter",
    "data"
  ],
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "YOUR-REPO-URL"
  },
  "activationEvents": [
    "onCommand:string-highlighter.convertJsonToEdn",
    "onCommand:string-highlighter.convertEdnToJson",
    "onCommand:string-highlighter.prettyPrintEdn"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "string-highlighter.convertJsonToEdn",
        "title": "Convert JSON to EDN"
      },
      {
        "command": "string-highlighter.convertEdnToJson",
        "title": "Convert EDN to JSON"
      },
      {
        "command": "string-highlighter.prettyPrintEdn",
        "title": "Pretty Print EDN"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "submenu": "string-highlighter.submenu",
          "group": "1_modification",
          "when": "editorHasSelection"
        }
      ],
      "string-highlighter.submenu": [
        {
          "command": "string-highlighter.convertJsonToEdn",
          "group": "2_conversion"
        },
        {
          "command": "string-highlighter.convertEdnToJson",
          "group": "2_conversion"
        },
        {
          "command": "string-highlighter.prettyPrintEdn",
          "group": "2_conversion"
        }
      ]
    },
    "submenus": [
      {
        "id": "string-highlighter.submenu",
        "label": "Convert Text"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/node": "^16.x.x",
    "@types/vscode": "^1.60.0",
    "@vscode/vsce": "^2.24.0",
    "eslint": "^8.x.x"
  },
  "dependencies": {
    "jsedn": "^0.4.1"
  }
}
